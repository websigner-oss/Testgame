# 🚀 البدء السريع - Quick Start Guide

## للاستخدام المحلي على الكمبيوتر:

### الطريقة 1: فتح مباشر (قد لا يعمل دائماً)
```
افتح ملف index.html في المتصفح مباشرة
```

### الطريقة 2: باستخدام Python (موصى بها)
```bash
# انتقل إلى مجلد المشروع
cd path/to/project

# شغّل الخادم المحلي
python -m http.server 8000

# افتح في المتصفح
http://localhost:8000
```

### الطريقة 3: باستخدام Node.js
```bash
# ثبّت http-server
npm install -g http-server

# شغّل الخادم
http-server -p 8000

# افتح في المتصفح
http://localhost:8000
```

### الطريقة 4: باستخدام PHP
```bash
php -S localhost:8000
```

## للاستخدام على الموبايل:

### الخيار 1: GitHub Pages (موصى به)
1. ارفع المشروع على GitHub
2. اذهب إلى Settings > Pages
3. اختر branch: main
4. احفظ وافتح الرابط على الموبايل

### الخيار 2: Netlify Drop
1. اذهب إلى https://app.netlify.com/drop
2. اسحب مجلد المشروع
3. افتح الرابط على الموبايل

### الخيار 3: عبر الشبكة المحلية
```bash
# على الكمبيوتر، شغّل:
python -m http.server 8000

# اعرف عنوان IP:
# Windows: ipconfig
# Mac/Linux: ifconfig أو ip addr

# على الموبايل، افتح:
http://[IP-ADDRESS]:8000
# مثال: http://192.168.1.100:8000
```

## اختبار سريع:

### إذا لم يعمل index.html:
```
1. افتح test-simple.html أولاً
2. إذا عمل test-simple.html: المشكلة في OpenCV.js
3. إذا لم يعمل: المشكلة في المتصفح
```

## استكشاف الأخطاء:

### المشكلة: "لا يحدث شيء عند رفع صورة"
```
✓ افتح Console (F12)
✓ ابحث عن رسائل الخطأ
✓ تأكد من أن JavaScript يعمل
✓ جرب test-simple.html
```

### المشكلة: "OpenCV.js لم يكتمل تحميله"
```
✓ انتظر 10-30 ثانية
✓ تحقق من اتصال الإنترنت
✓ جرب "تحسين الجودة" - يعمل بدون OpenCV
```

### المشكلة: "الصورة لا تظهر"
```
✓ تأكد من أن الملف صورة (JPG, PNG)
✓ جرب صورة أصغر
✓ تحقق من Console للأخطاء
```

## الملفات:

- `index.html` - التطبيق الرئيسي الكامل
- `test-simple.html` - اختبار بسيط (للتشخيص)
- `styles.css` - التنسيقات
- `app.js` - منطق التطبيق
- `README.md` - التوثيق الكامل
- `TROUBLESHOOTING.md` - دليل حل المشاكل
- `DEPLOYMENT.md` - دليل النشر

## الميزات المتاحة:

### ✅ يعمل بدون OpenCV:
- تحميل الصور
- معاينة الصور
- تحسين الجودة (معالجة بسيطة)
- تحميل الصور

### ⏳ يحتاج OpenCV.js:
- إصلاح الصورة (معالجة متقدمة)
- إزالة الضوضاء
- تحسين الجودة (معالجة متقدمة)
- إصلاح التشققات

## نصائح الأداء:

```
✓ استخدم صور أقل من 5 MB
✓ الأبعاد الموصى بها: أقل من 4000x4000
✓ OpenCV.js يُحمّل مرة واحدة ويُخزّن في cache
✓ أول استخدام قد يستغرق وقتاً أطول
```

## المتصفحات المدعومة:

```
✅ Chrome/Edge 90+
✅ Firefox 88+
✅ Safari 14+
✅ Samsung Internet 14+

⚠️ UC Browser (قد يواجه مشاكل)
❌ Internet Explorer (غير مدعوم)
```

---

💡 **للمزيد من التفاصيل:**
- راجع `README.md` للتوثيق الكامل
- راجع `TROUBLESHOOTING.md` لحل المشاكل
- راجع `DEPLOYMENT.md` لخيارات النشر

🎯 **الهدف السريع:**
```
1. شغّل: python -m http.server 8000
2. افتح: http://localhost:8000
3. ارفع صورة
4. اضغط "تحسين الجودة"
5. حمّل النتيجة
```

✨ **جاهز للاستخدام!**
